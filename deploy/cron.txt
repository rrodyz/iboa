# ═══════════════════════════════════════════════════════════════════════════════
# A3 ERP — Entrée Cron (Laravel Scheduler)
# ═══════════════════════════════════════════════════════════════════════════════
# Ajouter avec : crontab -e  (en tant que www-data)
#
# [PILOT-DEPLOY-02] Une SEULE entrée cron applicative. Laravel gère lui-même
# le calendrier de toutes les tâches planifiées (routes/console.php) — ne
# JAMAIS ajouter une tâche métier ici en doublon, le scheduler la lancerait
# deux fois. La rotation des logs n'est plus une entrée cron séparée non
# plus : LOG_CHANNEL=daily + LOG_DAILY_DAYS=14 (.env) fait déjà tourner et
# purger les logs nativement, sans tâche externe.
# ═══════════════════════════════════════════════════════════════════════════════

* * * * * www-data php /home/votre-domaine.com/public_html/artisan schedule:run >> /dev/null 2>&1

# ── Tâches planifiées définies dans routes/console.php ────────────────────────
# (Gérées automatiquement par le scheduler ci-dessus, NE JAMAIS les dupliquer
# en entrées cron séparées — liste tenue à jour manuellement, à vérifier
# contre routes/console.php si ce fichier change)
#
# 01:30 backup:clean                    — onOneServer() — purge des anciennes sauvegardes
# 02:00 backup:run                      — onOneServer() — sauvegarde complète DB + fichiers
# 05:30 audit:business                  — audit intégrité métier quotidien
# 05:45 automation:daily                — transitions de statut + alertes
# 05:50 audit:sync                      — détection rupture de chaîne inter-modules
# 05:55 sync:modules                    — resynchronisation des agrégats dénormalisés
# 06:00 invoices:mark-overdue           — marque les factures en retard
# 06:30 invoices:generate-recurring     — withoutOverlapping()+onOneServer() — factures récurrentes échues
# 07:00 backup:monitor                  — onOneServer() — vérifie fraîcheur/santé des sauvegardes
# 08:00 crm:notify-overdue-activities   — alertes activités CRM en retard
# 08:15 validations:remind              — relance des validations en attente
# horaire alerts:run                    — withoutOverlapping() — alertes par seuil
